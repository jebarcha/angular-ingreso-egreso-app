// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
	production: false,
	firebase: {
		apiKey: 'AIzaSyDHTahUpNHwgXL54wmAnUF6fFuu1wzI1j4',
		authDomain: 'ng-ingreso-egreso-app.firebaseapp.com',
		databaseURL: 'https://ng-ingreso-egreso-app.firebaseio.com',
		projectId: 'ng-ingreso-egreso-app',
		storageBucket: 'ng-ingreso-egreso-app.appspot.com',
		messagingSenderId: '739857812661',
		appId: '1:739857812661:web:afb83e286360b93e'
	}
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
